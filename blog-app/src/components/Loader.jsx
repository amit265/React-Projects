import React from 'react';

const Loader = () => {
  return (
    <div className="loader">
      {/* Customize loading indicator here (e.g., spinner, progress bar) */}
      Loading...
    </div>
  );
};

export default Loader;
